[package]
name = "carriage_run"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = "0.4"
macroquad-toolkit = { path = "../macroquad-toolkit" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
winres = "0.1"

[dev-dependencies]
# Already present through macroquad; direct test access verifies shipped glyphs.
fontdue = "0.9"
